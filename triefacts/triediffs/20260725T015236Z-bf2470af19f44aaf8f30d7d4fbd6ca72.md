<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## Wiki front door: `trie index` generates <triefacts.root>/README.md… — 2026-07-25 (parent 0455d3a4aec1)

Adds a `trie index` command that generates `<triefacts.root>/README.md` as a wiki front door. Three new symbols in `trie/index` implement this:

- `build_index` — renders the markdown with a managed marker, top public symbols ranked by inbound reference count (with one-liners), and a per-directory file TOC drawn from front-matter descriptions; skips the digest archive dir and the index file itself
- `write_index` — writes `README.md` to the triefact root; returns `None` when no tree exists
- `_file_descriptions` — single-pass front-matter collector over the triefact tree

Two CLI additions: `index_cmd` exposes `trie index` manually; `_refresh_index_quietly` is called

### Changes

- + trie/index:build_index — "Render the full triefact-tree index as a Markdown string from `store` and on-disk triefact files."
- + trie/index:write_index — "Write the generated index markdown to `<triefacts.root>/README.md`, returning the written path."
- + trie/index:_file_descriptions — "Scan all `.md` files under `triefacts_root` and return a mapping of triefact-relative paths to their YAML front-matter `description` field (empty string if…"
- + trie/cli:index_cmd — "Regenerate the triefact-tree index (`<triefacts.root>/README.md`) from the live graph store without calling the LLM."
- + trie/cli:_refresh_index_quietly — "Regenerate the triefact index after a sync, silently swallowing all exceptions."
- ~ trie/cli:_run_incremental_sync — Regenerates the index after a successful incremental sync.
- ~ trie/cli:_run_full_pass — "Executes first-run bootstrap sync: scans project, builds plan, prompts for confirmation, then generates triefacts." → "Executes first-run bootstrap sync: scans project, builds plan, prompts for confirmation, then generates triefacts and refreshes the index."
- ~ trie/session_diff:_triefact_pathspecs — "Return git pathspecs covering the triefact root while excluding `config.diff.diffs_dir` to prevent digest files from polluting evidence collection." → "Return git pathspecs covering the triefact root while excluding `config.diff.diffs_dir` and the root `README.md` to prevent digest files and the generated…"
- + tests/test_index:__module__ — "Tests for `trie.index` covering `build_index` output shape, `write_index` idempotence, and no-op behaviour when no triefact tree exists."
- + tests/test_index:_project — "Build a minimal fake project under `tmp_path` with source files, a populated `Store`, and a triefact tree, returning the `Config` and `Store` for index tests."
- + tests/test_index:test_build_index_shape — "Verify that `build_index` produces output containing the index marker, ranked public symbols with one-liners, per-directory file TOC with descriptions, and no…"
- + tests/test_index:test_write_index_and_idempotence — "Verify `write_index` writes `README.md` and produces identical output on a second invocation."
- + tests/test_index:test_write_index_without_tree_is_noop — "Assert that `write_index` returns `None` when no triefacts tree exists in the project root."
- + trie/index:INDEX_MARKER — "HTML comment string embedded in the generated `README.md` to mark it as trie-owned and warn against manual edits."
- + trie/index:_ENTRY_POINT_LIMIT — "Maximum number of entry-point symbols included in the index's "Entry points" section."
- + trie/index:__module__ — "Generate the wiki front door: a deterministic, LLM-free index `README.md` for the triefact tree, derived from the graph store and triefact front matter."
