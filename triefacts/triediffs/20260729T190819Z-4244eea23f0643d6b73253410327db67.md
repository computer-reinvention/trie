<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## Session changes — 2026-07-30 (parent 42d556bc148f)

**Bump trie package version from 0.2.0 to 0.2.1 to make installed-binary skew detectable via the gate's skew probe.**

The constant `trie/__init__:__version__` is incremented from `0.2.0` to `0.2.1`, in lockstep with `pyproject.toml`. This patch version makes the refresh→sync merge and interface coherence work from the previous session visible to the gate's skew probe, which compares the in-source version string against the installed binary to detect drift.

> **Intent vs. diff:** The patch note states the bump is from 0.2.0 to 0.2.1, but the triefact diff shows trie_version advancing from 0.1.9 to 0.2.0, not from 0.2.0 to 0.2.1 — suggesting the triefact was not previously in sync with the source version for `trie/__init__:__version__`.

### Changes

- ~ trie/__init__:__version__ — Version bump 0.2.0 -> 0.2.1, in lockstep with pyproject.toml (the gate's skew probe compares these against the installed binary).
