<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## Session changes — 2026-08-03 (parent b8ef97ccc41e)

**Bump trie package version from 0.2.1 to 0.2.2 to release opencode override, setup detection, Haiku default, and triediff rendering/truncation fixes.**

The constant `trie/__init__:__version__` is incremented from `0.2.1` to `0.2.2`. This release version consolidates several previously merged changes:

- opencode provider override support
- setup/environment detection improvements
- Claude Haiku as the new default model
- triediff rendering and truncation bug fixes

> **Intent vs. diff:** The patch note states the bump is from 0.2.1 → 0.2.2, but the triefact diff shows the version moving from 0.2.0 to 0.2.1 (trie_version field and implicitly __version__), suggesting the recorded triefact was one release behind and the actual source change may differ from the stated starting point.

### Changes

- ~ trie/__init__:__version__ — Bump package version 0.2.1 → 0.2.2 for the release covering the opencode override, setup detection, Haiku default, and triediff rendering/truncation fixes.
