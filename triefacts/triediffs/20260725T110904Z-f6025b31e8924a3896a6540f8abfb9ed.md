<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## README opener: after defining the two indexes, state concretely how agents use… — 2026-07-25 (parent 9081929650da)

README.md updated to explain agent usage immediately after the two index definitions. The new prose covers the read side — overridden `grep`/`read` returning prose summaries with one-liners, plus `trace` and `--history` for deeper inspection — and the write side: native editing, per-symbol notes, archive application, and the gate guaranteeing the next session's context is populated.

### Changes

- ~ README.md — Opener gains the agent read/write usage block right after the two index definitions.
