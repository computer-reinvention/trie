<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## Bump version 0.1.9 -> 0.2.0 for the multi-language + LSP resolver release. — 2026-07-29 (parent 706f9854f4b5)

Bumps the package version from 0.1.9 to 0.2.0 for the multi-language + LSP resolver release. Affected symbols:

- `trie/__init__:__version__` — string literal updated to `"0.2.0"`
- `trie/config:TrieMeta` — default `version` field and `DEFAULT_CONFIG_TOML` template string updated to `0.2.0`
- `tests/test_config:test_defaults_when_empty_dict` — expected version assertion updated to `0.2.0`
- `tests/test_config:test_load_roundtrips_default_template` — expected version assertion updated to `0.2.0`

The diff also shows `trie/mcp_server:TrieTools` role changed from `api` to `orchestration`, which is not mentioned in the patch notes.

### Changes

- ~ tests/test_config:test_defaults_when_empty_dict — "Verifies that Config.from_dict uses default values when initialized with empty dictionary." → "Verifies that `Config.from_dict` uses default values when initialized with empty dictionary."
- ~ tests/test_config:test_load_roundtrips_default_template — Update expected version assertion for the default TOML template to 0.2.0.
- ~ trie/__init__:__version__ — Bump package version 0.1.9 -> 0.2.0 for the multi-language + LSP resolver release.
- ~ trie/config:TrieMeta — Default version 0.1.9 -> 0.2.0 (and DEFAULT_CONFIG_TOML template version).
