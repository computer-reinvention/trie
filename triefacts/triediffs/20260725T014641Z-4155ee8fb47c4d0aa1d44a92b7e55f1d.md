<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## Wiki contributor ergonomics: the tampered_body verify label now instructs… — 2026-07-25 (parent 1dd088295cce)

Two user-facing changes were made. In `trie/cli.py`, the `_REASON_LABELS` dict's `TAMPERED_BODY` entry was reworded from an accusation to an actionable instruction telling users to move prose outside the sentinels and run `trie sync`. The sentinel ownership contract was also stated explicitly in README's hand-written notes section.

A new 'Adopting trie in a team' README section was added covering:
- who pays for LLM calls (payment patterns)
- the regenerate-don't-hand-merge rule with a `merge=ours` `.gitattributes` recipe
- privacy story: what is sent to providers vs. what stays local

Triefact updates reflect the `_REASON_LABELS` and `verify_cmd` source changes in `trie/cli.md`, plus routine `source_ref` and timestamp refreshes across `trie/mcp_server.md` and test triefacts.

### Changes

- ~ trie/cli:_REASON_LABELS — TAMPERED_BODY label reworded to an instruction: hand-edited inside a generated section — move prose outside the sentinels and run trie sync.
- ~ README.md — Sentinel ownership contract stated in Hand-written notes; new 'Adopting trie in a team' section (payment patterns, merge recipe, privacy).
