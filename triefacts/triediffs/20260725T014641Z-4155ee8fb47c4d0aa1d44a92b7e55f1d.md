<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## Wiki contributor ergonomics: the tampered_body verify label now instructs… — 2026-07-25 (parent 1dd088295cce)

`_REASON_LABELS` in `trie/cli` gains an actionable `TAMPERED_BODY` message instructing users to move prose outside the sentinels and run `trie sync`, replacing the previous accusatory label. The README receives an explicit sentinel ownership contract under "Hand-written notes" and a new "Adopting trie in a team" section covering:

- who pays for LLM calls in shared workflows
- a `merge=ours` `.gitattributes` recipe enforcing regenerate-don't-hand-merge
- a privacy summary of what is sent to providers vs kept local

Triefact roles for `verify_cmd`, `_REASON_LABELS`, `TrieTools`, and `TrieTools._staleness_notes` shift from `cli-

### Changes

- ~ trie/cli:_REASON_LABELS — TAMPERED_BODY label reworded to an instruction: hand-edited inside a generated section — move prose outside the sentinels and run trie sync.
- ~ README.md — Sentinel ownership contract stated in Hand-written notes; new 'Adopting trie in a team' section (payment patterns, merge recipe, privacy).
