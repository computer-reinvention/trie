<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## Brand-casing consistency sweep across README, TRIE.md template, QUICKSTART,… — 2026-08-01 (parent be79d9e9cd08)

**Updated two test assertions to match the corrected "Using Trie" brand casing in the bundled TRIE.md title.**

The bundled TRIE.md title follows the house naming convention — "Trie" capitalised in prose — so the string literal checked by two tests was updated accordingly.

- `test_install_creates_trie_md_at_project_root`: assertion that validates bundled content was loaded now looks for `"Using Trie"` instead of `"Using trie"`.
- `test_install_trie_md_updates_on_drift`: fingerprint updated to reflect the source change; the drift-restore logic is unchanged.

No behavioural logic was altered; the only effect is that the tests correctly reflect the canonical casing of the bundled document title.

### Changes

- ~ tests/test_docs_install:test_install_creates_trie_md_at_project_root — Assertion 'Using trie' -> 'Using Trie': bundled TRIE.md title follows the house naming convention (Trie in prose, backticked trie for commands, bare lowercase…
- ~ tests/test_docs_install:test_install_trie_md_updates_on_drift — Same convention update: drift-restore assertion tracks the new bundled title casing.
