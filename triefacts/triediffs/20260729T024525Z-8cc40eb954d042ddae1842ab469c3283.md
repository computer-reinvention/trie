<!-- auto-generated by `trie diff --write` (wired into the pre-commit hook)
     one immutable digest file per commit; TRIE_DIFF.md at the project root
     is a symlink to the latest one; do not edit by hand -->

## Merge origin/main into pg/tree-sitter-lsp-resolver; resolve TRIE_DIFF.md… — 2026-07-29 (parent d6005a8e620d)

Merges upstream main (commit 2a5cfca) into the `pg/tree-sitter-lsp-resolver` branch. The sole behavioural change brought in is to `_render_opencode_delete_symbol` in `trie/tool_override_install.py`: quotes inside the generated `delete_symbol` tool description are now escaped so the emitted opencode TypeScript config remains syntactically valid. All other diff content is metadata-only updates (source refs and symbol roles) propagated across every symbol in `tool_override_install` as a result of the merge commit changing the file fingerprint.

### Changes

- ~ trie/tool_override_install:_render_opencode_delete_symbol — Merged from main (2a5cfca): escape quotes in the generated delete_symbol tool description so the emitted opencode config stays valid.
